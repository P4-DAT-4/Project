fn int empty() {
    return 0
}

fn int add(int x, int y) {
    return x + y
}

fn int g(bool b, int a) {
    if b then
        a = 10
        return a
    else skip
}

fn [int] h([int] a) {
    while a[0] < 10 do
        a[0] = a[0] + 1
    return a
}
